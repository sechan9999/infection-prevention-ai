name: conformance

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  structural-conformance:
    name: Structural conformance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run conformance checks
        run: bash scripts/conformance.sh

      - name: Lint CLABSI fixtures
        run: python3 scripts/rule_replay.py

      - name: Summary
        if: always()
        run: |
          {
            echo "### Structural conformance"
            echo ""
            echo '```'
            NO_COLOR=1 bash scripts/conformance.sh || true
            NO_COLOR=1 python3 scripts/rule_replay.py || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
